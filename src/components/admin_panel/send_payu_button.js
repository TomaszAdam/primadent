import React from "react";
import styled from "styled-components";

const StyledButton = styled.button``;

const SendPayuButton = () => {
  return <StyledButton>wyślij ponownie link do płatności</StyledButton>;
};

export default SendPayuButton;
